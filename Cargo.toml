[package]
name = "rs-yolo26"
version = "0.1.0"
edition = "2021"
description = "Zero-dependency Rust reimplementation of Ultralytics YOLO26 inference"
license = "AGPL-3.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true